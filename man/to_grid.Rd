% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_grid.R
\name{to_grid}
\alias{to_grid}
\title{Pycnophylactic interpolation to a grid}
\usage{
to_grid(
  source,
  value_col,
  id_col = NULL,
  grid_type = c("h3", "a5", "s2", "raster", "isea3h", "isea4h", "isea7h", "isea43h"),
  resolution,
  cell_inclusion = c("intersect", "centroid"),
  cell_allocation = c("area", "centroid"),
  nb_order = 1,
  max_iter = 500,
  tolerance = 1e-04,
  include_self = TRUE,
  missing_policy = c("abort", "warn", "ignore")
)
}
\arguments{
\item{source}{An \code{sf} polygon object.}

\item{value_col}{Column containing the values to interpolate. May be
supplied as an unquoted column name or a character string.}

\item{id_col}{Optional unique identifier column for source polygons.}

\item{grid_type}{Grid system to interpolate to. One of \code{"h3"}, \code{"a5"},
\code{"s2"}, \code{"isea3h"}, \code{"isea4h"}, \code{"isea7h"}, \code{"isea43h"},  or \code{"raster"}.}

\item{resolution}{Grid resolution. For H3, A5, S2, and ISEA grids this is the grid
level. For raster grids this is the cell size in metres.}

\item{cell_inclusion}{Method used to determine which grid cells are included.
One of \code{"intersect"} or \code{"centroid"}.}

\item{cell_allocation}{Method used to allocate source values to grid cells.
One of \code{"area"} or \code{"centroid"}.}

\item{nb_order}{Neighbourhood order used for smoothing. A value of \code{1} uses
first-order neighbours; larger values include higher-order neighbours.}

\item{max_iter}{Maximum number of pycnophylactic smoothing iterations. If
\code{0}, returns the initial allocation without smoothing.}

\item{tolerance}{Convergence tolerance based on relative mean density change.}

\item{include_self}{Logical. Should each cell include itself when smoothing?}

\item{missing_policy}{How to handle source polygons that receive no grid
cells. One of \code{"abort"}, \code{"warn"}, or \code{"ignore"}.}
}
\value{
An \code{sf} object containing grid-cell geometries and interpolated
values.
}
\description{
Interpolates polygon counts to a regular or global discrete grid while
preserving source-zone totals.
}
