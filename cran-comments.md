## Test environments

* local macOS Tahoe 26.6.2, R 4.6.1
* win-builder (release)
* win-builder (devel)
* mac-builder (release)

## R CMD check results

There were no ERRORs, WARNINGs, or NOTEs.

## Package size

The package includes small example spatial datasets and pre-rendered figures used in examples and vignettes.

## Downstream dependencies

None (new package).
