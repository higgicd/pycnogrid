% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_isea.R
\name{to_isea}
\alias{to_isea}
\title{Pycnophylactic interpolation to an ISEA grid}
\usage{
to_isea(source, value_col, resolution, aperture = c(3, 4), ...)
}
\arguments{
\item{source}{An \code{sf} polygon object in a projected CRS.}

\item{value_col}{Column containing the values to interpolate. May be
supplied as an unquoted column name or a character string.}

\item{resolution}{ISEA resolution level. Higher values produce smaller cells
and substantially more output polygons.}

\item{aperture}{ISEA aperture: one of \code{3} or \code{4}.}

\item{...}{Additional arguments passed to \code{\link[=to_grid]{to_grid()}}.}
}
\value{
An \code{sf} object containing ISEA cells and interpolated values.
}
\description{
Interpolates polygon counts to an ISEA discrete global grid while preserving
source-zone totals.
}
\details{
ISEA grids are equal-area hierarchical discrete global grid systems based on
the Icosahedral Snyder Equal Area projection. The \code{aperture} determines the
refinement factor between successive resolutions: aperture 3, 4, or 7.

Calls \code{\link[=to_grid]{to_grid()}} with \code{grid_type} set to \code{"isea3h"} or \code{"isea4h"}
according to \code{aperture}.
}
\examples{
out <- to_isea(
  source = nyc_ct_small,
  value_col = populationE,
  resolution = 18,
  aperture = 3,
  max_iter = 5
)

out <- to_isea(
  source = nyc_ct_small,
  value_col = populationE,
  resolution = 14,
  aperture = 4,
  max_iter = 5
)

}
